const e=document.querySelector('#energy'),t=document.querySelector('#trade'),p=document.querySelector('#policy'),o=document.querySelector('#scenarioOut');function update(){if(!e||!t||!p||!o)return;const s=Math.round(Number(e.value)*.34+Number(t.value)*.33+Number(p.value)*.33);let label='Watch mode',note='Keep buffers, monitor triggers and avoid one static forecast.';if(s>=68){label='Stress mode';note='Use downside cases, protect liquidity and test margin sensitivity.'}if(s<=34){label='Stabilising mode';note='Use the base case, but keep trigger points visible.'}o.innerHTML='<strong>'+label+': '+s+'/100</strong><p>'+note+'</p>'} [e,t,p].forEach(x=>x&&x.addEventListener('input',update));update();